import xml.etree.ElementTree as ET

import logging
logger = logging.getLogger("geniv3-decode")


def decode_list_resources(xml_format):
    func_ = decode_list_resources.__name__
    #logger.debug("%s ingress=%s" % (func_, xml_format,))

    egress_ = []

    return egress_
